<fieldset>
    <div class="control-group">
        <label class="control-label">Emails</label>
        <div class="controls">
            <input type="text" name="check[multimail]" value="<%= check.getPollerParam('multimail') || '' %>" class="span8" placeholder="Emails to notify, comma separated"/>
        </div>
    </div>
</fieldset>
